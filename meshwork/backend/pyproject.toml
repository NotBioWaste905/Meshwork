[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "networkx>=3.4.2",
    "pydantic>=2.11.5",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
